title: Running AI/ML application tutorials for Life Sciences on LUMI

plugins:
  - jekyll-sitemap

collections:
   hands-on:
        output: true

defaults:
  -
    scope:
      path: "_hands-on"
    values:
      layout: "default"
